<div class="container">
  <div class="same-margin-top-text">
    <h1 class="top-header-page-black">Type of food</h1>
    <h2 class="top-text-page-black">What type of food would you like to cook? Let's select the ones you like 🤤🤤🤤</h2>

    <div id="create-favorites-form">
      <%= form_for current_user, url: favorite_cuisines_path, method: :post do |f| %>

        <%= f.collection_check_boxes :cuisine_ids , @cuisines, :id, :name, item_label_class: 'my-custom-class'   %>
        <%= f.submit 'Submit', class: "btn btn-lg btn-yellow px-4" %>
      <% end %>
    </div>
  </div>


  <div class="mt-3 d-flex flex-wrap justify-content-between">
    <% @cuisines.each do |cuisine| %>
      <div data-id="<%= cuisine.id %>" class="hover-darker-cuisine-card cuisine-card mb-3" >
        <div class="small-cuisine-card-image">
          <%= image_tag "#{cuisine.photo}", class:"small-cuisine-card-image", alt: "#{cuisine.name}" %>
          <div class="to-select"></div>

          <p class="small-cuisine-card-name-white"> <%= cuisine.name %></p>
        </div>
      </div>
    <% end %>
  </div>
</div>


